# Security — Lean Check — 2026-08-15

Lean check 2026-08-15 (file-based, no prod DB): 002 `increment_vote`/`decrement_vote` RPC atomic intact — `app/lib/store.ts:410` `supa.rpc` with hard throw, empty `update({})` fallback removed (burst 10/10 after 347f22c); 004 `poll-images` bucket `public=true` + `public read`/`service write` on `storage.objects` intact, `POST /api/polls` offloads data URLs; 003 RLS intact — `anon read` SELECT only on polls/poll_options/votes/events, `service write` via `supaService()`; server `Set-Cookie` now `HttpOnly; Secure; SameSite=Lax` in `app/api/polls/route.ts`/`app/api/polls/[id]/vote/route.ts` (client `app/lib/dedup.ts:27` helpers still JS-readable), `clientIpFromHeaders` now prefers `x-vercel-forwarded-for`/`x-real-ip` over `x-forwarded-for`; `.gitignore` covers `.env`/`.env.local`/`.env.*.local`/`supabase/.env`, no secrets staged. Remains: `IP_HASH_SALT||"dev-salt"` fallback, unbounded `image_url`/`meta` size, `events poll_id`/`ref` unvalidated. Security stays PARTIAL until live anon probe verifies 003/004 in prod.
