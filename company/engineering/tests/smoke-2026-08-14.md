# Smoke 2026-08-14 — PollPop Storage upload (004)

**Prod:** https://pollpop-five.vercel.app, Supabase dgurslguhkatnshlzvfcy, bucket `poll-images` (004 applied)

**Files:** `app/app/api/polls/route.ts` — data URL → Buffer → `supa.storage.from("poll-images").upload()` → `getPublicUrl` when `isSupabaseConfigured` (fallback keeps data:). `app/lib/store.ts` passthrough, `app/supabase/migrations/004_storage.sql` VERIFIED on disk.

**Probed via node fetch (curl denied, WebFetch model unavailable):**
- `GET /api/metrics` → 200 VERIFIED `{"totals":{"polls":0,"votes":0},"derived":{"ctr_poll_view":0,"k_factor":0}}` — healthy but DB empty.
- `GET /p/fit-check` → 200 HTML shell VERIFIED; `GET /api/polls/fit-check` → 404 `Poll not found` — seeds are mock-only, not in Supabase.
- `POST /api/polls` picsum URLs → 500 `{"error":"TypeError: fetch failed"}` VERIFIED — Supabase DB write fails.
- `POST /api/polls` 1x1 png data: URL ×2 → 500 `{"error":"Image upload failed — try again"}` VERIFIED — enters Storage branch (`route.ts:101`) then fails; confirms `isSupabaseConfigured=true` but upload broken.
- `POST /api/events` → 200 `{"ok":true}` VERIFIED.

**ESTIMATE vs VERIFIED:** 004 SQL VERIFIED locally; bucket existence INFERRED (no API to list). Upload wiring live VERIFIED; `https://.../poll-images/...` public URL NOT VERIFIED (no 201). Metrics CTR/K shape VERIFIED.

**Verdict:** 004 INFERRED not VERIFIED live. Needs manual: Supabase Dashboard → Storage → `poll-images` for object, Vercel env `SUPABASE_SERVICE_ROLE_KEY` + `vercel logs` for `[poll-images upload] failed` and `fetch failed` detail.
