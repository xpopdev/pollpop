# Perf Bench — 2026-08-14 — PollPop §33 — STUB — ESTIMATE

> NOT YET MEASURED — STUB. Method defined, not yet measured live due to prod DB block (1ee6279 `POST /api/polls` 500 `fetch failed`). All ESTIMATE per §29 until live probe fills Measured. See `company/engineering/tests/smoke-2026-08-14.md`.

| Target | Method | Budget | Measured |
|---|---|---|---|
| Create poll landing→share-link (4G mobile) | Playwright timed: `Date.now()` at landing → share-link visible, 4G throttled | ≤15s | NOT YET MEASURED |
| Vote optimistic / confirmed | `Date.now()` before/after `POST /api/polls/:id/vote` (paint vs `counts` ack) | <500ms / <1.5s | NOT YET MEASURED |
| Realtime tally propagation | Two tabs on `p/{id}` via `poll:{id}` Realtime channel; fallback polling `GET /api/polls/:id` | <2s / 5s fallback | NOT YET MEASURED |
| Burst 50 concurrent votes | `Promise.all` 50 POSTs with distinct `voter_cookie`/`ip_hash`; sum `poll_options.votes` == 50 | exactly 50 | NOT YET MEASURED |
| OG edge cache | `curl -I .../og` check `Cache-Control: public, max-age=3600` | 3600 | NOT YET MEASURED |

**Status:** NOT YET MEASURED — stub (method defined, not yet measured live due to prod DB block).

**Note:** Bench is stub — method/budget defined, measurement blocked by prod DB (1ee6279). No live timing until Supabase write + `poll-images` upload verified.

**Next:** After 004 verified (`vercel logs` + Supabase Dashboard Storage check), `npm run test:e2e` timed on `https://pollpop-five.vercel.app` + `curl -w %{time_total} https://pollpop-five.vercel.app/api/polls` + Supabase Table Editor row timing.
