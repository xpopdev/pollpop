# agent_handoffs/

One file per handoff, named `YYYY-MM-DD_<from>_to_<to>.md`, in the exact format from master
protocol §13. Use the `handoff` skill to create these rather than writing them free-hand, so
the format stays consistent. The next agent must read the relevant handoff before starting.
