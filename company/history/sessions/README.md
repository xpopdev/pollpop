# sessions/

Optional per-session notes, if a session's context is worth preserving beyond what
`audit_log.md` and the handoff files already capture. Most sessions don't need a file here —
use it when a session involved enough back-and-forth that a plain audit-log line would lose
important nuance.
