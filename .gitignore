# Secrets — never commit (gitignored; only .env.example is tracked)
.env
.env.local
.env.*.local
supabase/.env
pollpop-validation/.env
.x_cookies.json
.x_cookies*
x_cookies.json
cookies.json
cookies.txt

# Personal Claude Code overrides — never shared
.claude/settings.local.json
CLAUDE.local.md

# Autonomy cross-check hook's debounce cache — machine/session-local, not meaningful to share
.claude/.autonomy-check-cache

# OS / editor noise
.DS_Store
*.swp

# App mock store (file fallback for mock mode when Supabase not configured)
app/.pollpop-mock.json
.pollpop-mock.json

# Common dependency / build output
node_modules/
__pycache__/
*.pyc
.venv/
dist/
build/
.next/
app/.next/
*.tsbuildinfo
app/tsconfig.tsbuildinfo
